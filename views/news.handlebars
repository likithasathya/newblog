<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>News Headlines for {{ date }}</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <div class="container">
        <h1>News Headlines for {{ date }}</h1>
        {{#each articles}}
            <div class="news-block">
                <h2><a href="{{ url }}" target="_blank">{{ title }}</a></h2>
                <p class="published-at">{{ publishedAt }}</p>
                {{#if imageUrl}}
                <img src="{{ imageUrl }}" alt="{{ title }}" class="article-image">
                {{/if}}
                <p>{{ description }}</p>
            </div>
        {{/each}}
        <p><a href="/">Back to Home</a></p>
    </div>
</body>
</html>
